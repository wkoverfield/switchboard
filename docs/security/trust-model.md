# Trust Model

Switchboard is local-first. Remote telemetry is planned to be opt-in only. Provider secrets should not be stored in repo config or agent MCP config.

Milestone 0/1 validates config and detects namespace collisions. Stronger enforcement, secrets, approvals, audit logs, and daemon socket security are later milestones.
